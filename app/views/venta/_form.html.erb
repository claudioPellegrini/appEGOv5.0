<%= form_for(ventum) do |f| %><div class="panel panel-default">    <div class="panel-body">      <% if ventum.errors.any? %>        <div id="error_explanation">          <h2><%= pluralize(ventum.errors.count, "error") %> prohibited this ventum from being saved:</h2>          <ul>          <% ventum.errors.full_messages.each do |message| %>            <div class="alert alert-danger"><%= message %></div>          <% end %>          </ul>        </div>      <% end %>      <div class="panel-body form-group">        <p><%= f.label :menu_id %></p>        <%= f.select :menu_id,  Menu.all.collect {|t| [ t.fecha, t.id ] }, { include_blank: 'Seleccionar Menu' }, class: 'form-control' %>      </div>      </br>            <div class="panel-body form-group">        <p><%= f.label :bebida_id %></p>        <%= f.select :bebida_id,  Bebida.all.collect {|t| [ t.nombre, t.id ] }, { include_blank: 'Seleccionar Bebida' }, class: 'form-control' %>      </div>      </br>            <!-- <div class="panel-body form-group">        <h1>Probando1</h1>        <% if ventum.menu_id.present? %>        <h1>Probando2</h1>        <% menu_id.productos.tipos.each do |t| %>                                  <div class="field">                <%= check_box_tag "productos[]", producto.id, chequeada(producto.id)  %> <%= producto.nombre %>                              </div>              <br>                  <%end%>        <%end%>      </div> -->      </br>      <div class="col-md-12">          <div class="btn-group btn-group-justified">                        <div class="btn-group">            <%= f.submit "Guardar Venta", class: 'btn btn-info ' %>            </div>            <div class="btn-group">            <%= link_to 'Cancelar', venta_path, class: 'btn btn-info'  %>            </div>          </div>      </div>      </div>      </div>    <% end %>